The example sources found in the subdirectories of this directory is
small, self-contained Ww- or SoWw-specific tests.

Compilation after a correct SoWw-installation on your system is done
by executing

        $ soww-config --build <executablename> <sourcefile.cpp>

(``soww-config'' must be in your $PATH).

The sources are all under the 3-clause BSD license.


--
Fabrizio Morciano
mortene@sim.no
